# 本次变更报告

插件现在支持通过 `cooldown_seconds` 设置全局冷静期。默认值为 `0`；例如设置为 `5` 时，机器人从开始规划回复起锁住所有唤醒，直到最后一条回复发送完成后再冷静 5 秒，期间新唤醒直接丢弃。

已同步更新配置 schema、README 和测试，并完成可用环境下的静态与 mock 验证。完整 pytest 需要在安装 pytest 和 AstrBot 运行时依赖的环境中执行。
